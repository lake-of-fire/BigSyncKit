name: Worker 1 source publication handoff publish

on:
  push:
    branches:
      - codex/worker1-transport-publish-20260910

permissions:
  contents: read

concurrency:
  group: worker1-source-publication-pr8
  cancel-in-progress: true

jobs:
  supersede:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseding incomplete source-publication candidate before publication."
