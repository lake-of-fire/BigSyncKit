name: Worker 1 staging (dormant)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dormant:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Staging branch is dormant between guarded publications."
