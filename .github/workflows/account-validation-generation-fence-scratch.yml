name: Account validation generation fence scratch

on:
  push:
    branches: [automation/account-validation-generation-fence-20260913]

permissions:
  contents: write

jobs:
  patch-and-qualify:
    runs-on: macos-15
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          path: BigSyncKit

      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: lake-of-fire/RealmSwiftGaps
          ref: 2d4fa2bfd8b1c856b45aca2c7a97c301d37204e2
          path: RealmSwiftGaps
          persist-credentials: false

      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: lake-of-fire/SwiftUtilities
          ref: f437c7d06fc631cd7a67731279411c417cdf8077
          path: SwiftUtilities
          persist-credentials: false

      - name: Patch current main and qualify account fencing
        working-directory: BigSyncKit
        env:
          TARGET_BRANCH: fix/account-validation-generation-fence-20260913
        run: |
          set -euo pipefail
          cp .github/scripts/account_validation_generation_fence_patch.py /tmp/account_validation_generation_fence_patch.py
          git fetch origin main
          BASE_SHA="$(git rev-parse origin/main)"
          git checkout --detach "$BASE_SHA"
          python3 /tmp/account_validation_generation_fence_patch.py
          git diff --check
          swift test --filter CloudKitSynchronizerAccountFencingTests
          git config user.name "OpenAI Codex"
          git config user.email "codex@users.noreply.github.com"
          git add Sources/BigSyncKit/QSSynchronizer/CloudKitSynchronizer.swift
          git commit -m "Fence account validation by authority generation"
          git push --force-with-lease origin "HEAD:refs/heads/${TARGET_BRANCH}"
